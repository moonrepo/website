---
title: TaskConfig
---

Configures a task to be ran within the action pipeline.

## Index

| Property | Type | Description |
| --- | --- | --- |
| [`args`](#args) | [`TaskArgs`](./TaskArgs.mdx) | Arguments to pass to the command when it's ran. Can be defined as a string, or a list of individual arguments. |
| [`checks`](#checks) | `TaskCheck[]` | Checks are shell scripts that run before the task is executed, and are categorized into three types: conditions, requirements, and fingerprints. Depending on the result of the script and the type of check, the task may be skipped, will fail, or continue. @since 2.4.0 |
| [`command`](#command) | [`TaskArgs`](./TaskArgs.mdx) | The command line to execute when the task is ran. Supports the command (executable) with or without arguments. Can be defined as a string, or a list of individual arguments. |
| [`deps`](#deps) | `TaskDependency[]` | Other tasks that this task depends on, and must run to completion before this task is ran. Can depend on sibling tasks, or tasks in other projects, using targets. |
| [`description`](#description) | `string` | A human-readable description about the task. @since 1.22.0 |
| [`env`](#env) | `Record<string, string \| null>` | A map of environment variables that will be set in the child process when the task is ran. |
| [`extends`](#extends) | [`Id`](./Id.mdx) | Extends settings from a sibling task by identifier. |
| [`inputs`](#inputs) | `Input[]` | A list of inputs that will be hashing and compared against changed files to determine affected status. If affected, the task will run, otherwise it will exit early. An input can be a literal file path, a glob pattern, environment variable, and more. |
| [`options`](#options) | [`TaskOptionsConfig`](./TaskOptionsConfig.mdx) | Options to control task inheritance, execution, and more. |
| [`outputs`](#outputs) | `Output[]` | A list of outputs that will be created when the task has successfully ran. An output can be a literal file path, or a glob pattern. |
| [`preset`](#preset) | [`TaskPreset`](./TaskPreset.mdx) | The preset to apply for the task. Will inherit default options. |
| [`script`](#script) | `string` | A script to run within a shell. A script is anything from a single command, to multiple commands, or shell specific syntax. Does not support arguments, merging, or inheritance. This overrides `command` and `args`. @since 1.27.0 |
| [`tags`](#tags) | `Id[]` | A list of tags to categorize this task with. Tags are inherited and can be used for querying and filtering tasks. @since 2.3.0 |
| [`toolchains`](#toolchains) | `Id \| Id[]` | A toolchain, or list of toolchains, in which the task will inherit functionality from. |
| [`type`](#type) | [`TaskType`](./TaskType.mdx) | The type of task, primarily used for categorical reasons. When not provided, will be automatically determined based on configured outputs. |

## Properties

### `args`

> **Nullable**

Arguments to pass to the command when it's ran. Can be
defined as a string, or a list of individual arguments.

| Attribute | Value |
| --- | --- |
| Type | [`TaskArgs`](./TaskArgs.mdx) |

### `checks`

> **Optional** · **Nullable**

Checks are shell scripts that run before the task is executed,
and are categorized into three types: conditions, requirements,
and fingerprints. Depending on the result of the script and the
type of check, the task may be skipped, will fail, or continue.
@since 2.4.0

| Attribute | Value |
| --- | --- |
| Type | `TaskCheck[]` |
| References | [`TaskCheck`](./TaskCheck.mdx) |

### `command`

> **Nullable**

The command line to execute when the task is ran.
Supports the command (executable) with or without arguments.
Can be defined as a string, or a list of individual arguments.

| Attribute | Value |
| --- | --- |
| Type | [`TaskArgs`](./TaskArgs.mdx) |

### `deps`

> **Optional** · **Nullable**

Other tasks that this task depends on, and must run to completion
before this task is ran. Can depend on sibling tasks, or tasks in
other projects, using targets.

| Attribute | Value |
| --- | --- |
| Type | `TaskDependency[]` |
| References | [`TaskDependency`](./TaskDependency.mdx) |
| Aliases | `dependsOn` |

### `description`

> **Optional** · **Nullable**

A human-readable description about the task.
@since 1.22.0

| Attribute | Value |
| --- | --- |
| Type | `string` |

### `env`

> **Optional** · **Nullable**

A map of environment variables that will be set in the child
process when the task is ran.

| Attribute | Value |
| --- | --- |
| Type | `Record<string, string \| null>` |

### `extends`

> **Optional** · **Nullable**

Extends settings from a sibling task by identifier.

| Attribute | Value |
| --- | --- |
| Type | [`Id`](./Id.mdx) |

### `inputs`

> **Optional** · **Nullable**

A list of inputs that will be hashing and compared against changed files
to determine affected status. If affected, the task will run, otherwise
it will exit early. An input can be a literal file path, a glob pattern,
environment variable, and more.

When not provided, all files within the project are considered inputs.
When an empty list, no files are considered. Otherwise, an
explicit list of inputs are considered.

| Attribute | Value |
| --- | --- |
| Type | `Input[]` |
| References | [`Input`](./Input.mdx) |

### `options`

> **Required**

Options to control task inheritance, execution, and more.

| Attribute | Value |
| --- | --- |
| Type | [`TaskOptionsConfig`](./TaskOptionsConfig.mdx) |

### `outputs`

> **Optional** · **Nullable**

A list of outputs that will be created when the task has successfully ran.
An output can be a literal file path, or a glob pattern.

| Attribute | Value |
| --- | --- |
| Type | `Output[]` |
| References | [`Output`](./Output.mdx) |

### `preset`

> **Optional** · **Nullable**

The preset to apply for the task. Will inherit default options.

| Attribute | Value |
| --- | --- |
| Type | [`TaskPreset`](./TaskPreset.mdx) |
| Values | `"utility"`, `"server"` |

### `script`

> **Optional** · **Nullable**

A script to run within a shell. A script is anything from a single command,
to multiple commands, or shell specific syntax. Does not support
arguments, merging, or inheritance. This overrides `command` and `args`.
@since 1.27.0

| Attribute | Value |
| --- | --- |
| Type | `string` |

### `tags`

> **Optional** · **Nullable**

A list of tags to categorize this task with. Tags are inherited and can
be used for querying and filtering tasks.
@since 2.3.0

| Attribute | Value |
| --- | --- |
| Type | `Id[]` |
| References | [`Id`](./Id.mdx) |

### `toolchains`

> **Optional** · **Nullable**

A toolchain, or list of toolchains, in which the task will inherit
functionality from.

| Attribute | Value |
| --- | --- |
| Type | `Id \| Id[]` |
| References | [`Id`](./Id.mdx) |
| Aliases | `toolchain` |

### `type`

> **Optional** · **Nullable**

The type of task, primarily used for categorical reasons. When not provided,
will be automatically determined based on configured outputs.

| Attribute | Value |
| --- | --- |
| Type | [`TaskType`](./TaskType.mdx) |
| Default | `"test"` |
| Values | `"build"`, `"run"`, `"test"` |

## References

- [`Id`](./Id.mdx)
- [`Input`](./Input.mdx)
- [`Output`](./Output.mdx)
- [`TaskArgs`](./TaskArgs.mdx)
- [`TaskCheck`](./TaskCheck.mdx)
- [`TaskDependency`](./TaskDependency.mdx)
- [`TaskOptionsConfig`](./TaskOptionsConfig.mdx)
- [`TaskPreset`](./TaskPreset.mdx)
- [`TaskType`](./TaskType.mdx)
