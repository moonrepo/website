---
title: WorkspaceProjectsConfig
---

Using both glob patterns and file source paths.

## Index

| Property | Type | Description |
| --- | --- | --- |
| [`globFormat`](#globformat) | [`WorkspaceProjectGlobFormat`](./WorkspaceProjectGlobFormat.mdx) | The project identifier format for glob located projects. @since 2.0.0 |
| [`globs`](#globs) | `string[]` | A list of glob patterns in which to locate project directories. Can be suffixed with a `moon.*` config file to only find distinct projects. |
| [`sources`](#sources) | `Record<Id, string>` | A map of project identifiers to relative file paths to each project directory. |

## Properties

### `globFormat`

> **Required**

The project identifier format for glob located projects.
@since 2.0.0

| Attribute | Value |
| --- | --- |
| Type | [`WorkspaceProjectGlobFormat`](./WorkspaceProjectGlobFormat.mdx) |
| Default | `"dir-name"` |
| Values | `"dir-name"`, `"source-path"` |

### `globs`

> **Required**

A list of glob patterns in which to locate project directories.
Can be suffixed with a `moon.*` config file to only find distinct projects.

| Attribute | Value |
| --- | --- |
| Type | `string[]` |

### `sources`

> **Required**

A map of project identifiers to relative file paths to each project directory.

| Attribute | Value |
| --- | --- |
| Type | `Record<Id, string>` |
| References | [`Id`](./Id.mdx) |

## References

- [`Id`](./Id.mdx)
- [`WorkspaceProjectGlobFormat`](./WorkspaceProjectGlobFormat.mdx)
